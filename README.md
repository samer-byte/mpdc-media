# mpdc-media
Video + poster files embedded on morepartiesdc.com (Squarespace code injection).
Source of truth: ~/CC/web-studio/sites/mpdc/ (VIDEO_LIBRARY.md). Files are versioned by folder (v1/) so a re-encode never breaks a live page — add v2/, then switch the injection.
